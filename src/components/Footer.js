import React, { Component } from 'react'

export default class Footer extends Component {
    render() {
        return (
            <footer className="footer full-width">
                Made with 💜 &nbsp;using sass.
            </footer>
        )
    }
}
